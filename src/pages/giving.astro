---
interface Props {
  url: string;
}

const { url } = Astro.props;

const processUrl = (url: string) => {
  // function body
};
---

<script>
  window.location.href = "{url}";
</script>

<p>If you are not redirected, <a href={url}>go home</a>.</p>
